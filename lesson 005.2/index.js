console.log('Lesson 001');

(() => {
    // Duplicate filter
// function DuplicateFilter(array) {
//     let filteredArray = array.filter((item, index) => array.indexOf(item) === index);
//     return filteredArray
// }

// alert(DuplicateFilter([1, 2, 3, 4, 2, 3, 5]))
})();

(() => {
    // Codewars task
//  expected reversed string world=>dlrow
   
    // function solution(str){
    //     let newStr = ""
    //     for(let i = 0; i < str.length; i++){
    //       newStr = newStr + str.at(-i-1)
    //       }
    //     return newStr
    //   }

})();

(() => {
    // 


})();
